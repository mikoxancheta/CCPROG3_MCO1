public class Chicken extends Product{
    public Chicken(
        String productType,
        String productName,
        String serialNumber,
        double price
    ) {
        super("Chicken", productName, serialNumber, price);
    }
}
